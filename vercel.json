```json
{
    "buildCommand": "cd nhl-analytics && npm run build",
    "outputDirectory": "nhl-analytics/dist",
    "framework": "vite",
    "rewrites": [
        {
            "source": "/api/:path*",
            "destination": "/api/index.py"
        },
        {
            "source": "/(.*)",
            "destination": "/nhl-analytics/dist/$1"
        }
    ]
}
```