name: "CodeQL Configuration"

# Disable C/C++ analysis since this repository doesn't contain C/C++ code
# Only analyze languages that actually exist in the repository
disable-default-queries: false

paths-ignore:
  - '**/*.c'
  - '**/*.cpp'
  - '**/*.h'
  - '**/*.hpp'
  - '**/*.cc'
  - '**/*.cxx'

